О чем работа?

Во первых надо просмотреть файл "Cross sale k-drivers_v2_1.csv" в котором есть данные про клиентов банка.
Наша задача при помощи алгоритмов Машинного обучения воспроизвести код при помощи которого можно будем предугадать
столбик "категория".
Во вторых надо обустроить файл так чтобы наш алгоритм мог работать над файлом. Надо посмотреть есть ли пробелы в файле,
если есть пробелы, то надо узнать есть ли в них важная информация, если нет`то можно их удалить, если да`то надо сделать Feature engineering.
Для этого мы разработали функцию которая обрабатывает файл (feature_enginnering).
А потом надо выбрать такой алгоритм который подходит лучше всех. Сначала мы с Максимом выбрали Random Forest Classificator.
После этого мы решили попробовать Boosting Methods. Сначала я протестировал код без каких либо изменений в гиперпараметрах.
А после этого я изпользовал цикл for для того чтобы вычислить какой наилучшей цифрой будет для n_estimators.
код`

error_rates = []
for n in range(1,100):
    model = AdaBoostClassifier(n_estimator=n)
    model.fit(X_train, y_train)
    pred = model.predict(X_test)
    err = 1 - accuarcy_score(y_test, pred)
    error_rates.append(err)
print(error_rates)

При помощи этого кода я выбрал наилучшую цифру для этого гиперпараметра.
И напоследок, я написал функцию которая забирает модель алгоритма и данные файла и возврашает насколько были правы предсказания кода.